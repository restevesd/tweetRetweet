"text" TEXT,
"favorited" INTEGER,
"favoriteCount" REAL,
"replyToSN" INTEGER,
"created" REAL,
"truncated" INTEGER,
"replyToSID" INTEGER,
"id" TEXT,
"replyToUID" INTEGER,
"statusSource" TEXT,
"screenName" TEXT,
"retweetCount" REAL,
"isRetweet" INTEGER,
"retweeted" INTEGER,
"longitude" INTEGER,
"latitude" INTEGER 
